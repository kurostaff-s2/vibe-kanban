{
  "name": "council-kanban",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @vibe/local-web dev",
    "build": "pnpm --filter @vibe/local-web build",
    "check": "pnpm --filter @vibe/local-web check",
    "lint": "pnpm --filter @vibe/local-web lint",
    "format": "pnpm --filter @vibe/local-web format"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.7.0",
    "vite": "^7.3.1"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@10.13.1"
}
